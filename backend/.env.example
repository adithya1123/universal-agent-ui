# Databricks — Service Principal OAuth (recommended)
# Used by both the supervisor client and Databricks SDK
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_CLIENT_ID=
DATABRICKS_CLIENT_SECRET=

# Databricks — PAT (fallback)
# DATABRICKS_TOKEN=dapi_your_token_here

# Databricks — CLI auth (local dev fallback)
# DATABRICKS_CONFIG_PROFILE=DEFAULT

# Lakebase (provisioned instance)
LAKEBASE_INSTANCE_NAME=

# Lakebase (autoscaling) — used by supervisor client for dual-store persistence
LAKEBASE_AUTOSCALING_PROJECT=
LAKEBASE_AUTOSCALING_BRANCH=

# UC Volume path for CSV extraction from sub-agent results
RESULT_VOLUME_PATH=

# Agent config
MAX_HISTORY=
SUPERVISOR_TIMEOUT=

# MLflow
MLFLOW_EXPERIMENT_ID=

# Tool approval config — auto-approves MCP tool calls from the Supervisor
AUTO_APPROVE_TOOLS=True
